/**
 * Timeline utilities remain in @g-motion/shared for now to keep reusable,
 * package-agnostic timeline data structures available to multiple layers.
 */
export { TimelineTracksMap } from './track-map';
