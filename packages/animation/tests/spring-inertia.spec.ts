import { describe, it } from 'vitest';

describe('Spring + Inertia Handoff', () => {
  it.todo('carries final spring velocity into inertia segment');
  it.todo('honors spring mode precedence (mark overrides track)');
});
